---
phase: 11-cloud-services-deployment
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - deploy/deploy.sh
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-04

user_setup:
  - service: gcloud
    why: "Cloud Run and Cloud SQL deployment requires authenticated gcloud CLI"
    env_vars:
      - name: DB_PASS
        source: "User chooses a password for the Cloud SQL spatialhub user"
      - name: SECRET_KEY
        source: "Generated by deploy script if not provided, but user should store it for future redeploys"
    dashboard_config:
      - task: "Ensure gcloud CLI is authenticated to project interviewing-457222"
        location: "Terminal: gcloud auth login && gcloud config set project interviewing-457222"

must_haves:
  truths:
    - "Cloud SQL PostgreSQL instance exists in us-central1 with all Django tables migrated"
    - "habitat_zones table has 4 seeded zones"
    - "Django API responds at Cloud Run URL with data from Cloud SQL"
    - "Frontend loads at Firebase Hosting URL and connects to Cloud Run API"
    - "POST /api/sensor-ingest/ on Cloud Run stores a row in Cloud SQL"
    - "Deploy script is idempotent -- safe to re-run for future phase redeployments"
  artifacts:
    - path: "deploy/deploy.sh"
      provides: "Idempotent deploy script for Cloud SQL + Cloud Run + Firebase"
      min_lines: 80
  key_links:
    - from: "deploy/deploy.sh"
      to: "Cloud SQL instance"
      via: "gcloud sql instances create with db-f1-micro tier"
      pattern: "gcloud sql instances create"
    - from: "deploy/deploy.sh"
      to: "Cloud Run service"
      via: "gcloud run deploy --source . with env vars"
      pattern: "gcloud run deploy"
    - from: "deploy/deploy.sh"
      to: "Firebase Hosting"
      via: "VITE_API_URL baked at build time, firebase deploy"
      pattern: "firebase deploy --only hosting"
    - from: "deploy/deploy.sh"
      to: "Django migrations on Cloud SQL"
      via: "Local machine python manage.py migrate with DB_HOST pointed at Cloud SQL public IP"
      pattern: "manage.py migrate"
---

<objective>
Create the idempotent deploy script and execute a full deployment to GCP -- Cloud SQL provisioned, Django on Cloud Run, frontend on Firebase Hosting.

Purpose: This is the actual deployment step. Plan 01 prepared the codebase for production. This plan creates the automation to provision infrastructure and deploy, then verifies everything works at production URLs. After this plan, the website is live in the cloud.

Output: deploy/deploy.sh script and live production deployment verified by human.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cloud-services-deployment/11-CONTEXT.md
@.planning/phases/11-cloud-services-deployment/11-RESEARCH.md
@.planning/phases/11-cloud-services-deployment/11-01-SUMMARY.md

<interfaces>
<!-- Key configuration values from CONTEXT.md locked decisions -->

GCP Project: interviewing-457222
Region: us-central1
Cloud SQL tier: db-f1-micro
Cloud SQL: PostgreSQL, public IP, no Auth Proxy
Cloud Run: source-based deploy (gcloud run deploy --source .)
Firebase Hosting URL: interviewing-457222.web.app
Firebase config: spatialhub-frontend/firebase.json + .firebaserc (already configured)

From django_backend/spatialhub_backend/settings.py (after Plan 01):
```python
SECRET_KEY = os.environ.get('SECRET_KEY', 'django-insecure-local-dev-only-key')
DEBUG = os.environ.get('DEBUG', 'False') == 'True'
ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', '*').split(',')
# PostgreSQL branch:
'HOST': os.environ.get('DB_HOST'),       # no fallback
'NAME': os.environ.get('DB_NAME', 'spatialhub_db'),
'USER': os.environ.get('DB_USER', 'spatialhub'),
'PASSWORD': os.environ.get('DB_PASS'),   # no fallback
```

From Dockerfile (after Plan 01):
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY django_backend/ .
RUN pip install --upgrade pip && pip install -r requirements.txt
ENV USE_SQLITE=1
RUN python manage.py collectstatic --noinput
ENV USE_SQLITE=0
EXPOSE 8080
CMD ["gunicorn", "spatialhub_backend.wsgi:application", "--bind", "0.0.0.0:8080"]
```

From spatialhub-frontend/src/api/api.ts (after Plan 01):
```typescript
export const BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api';
```

Existing management commands:
- seed_habitat_zones (seeds 4 Mars habitat zones, idempotent via update_or_create)
- migrate (standard Django)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent deploy script and execute deployment</name>
  <files>deploy/deploy.sh</files>
  <action>
Create `deploy/deploy.sh` -- a single idempotent bash script that provisions all GCP infrastructure and deploys both backend and frontend. The script must be safe to re-run (subsequent runs skip provisioning, only redeploy code).

**Script structure:**

```bash
#!/usr/bin/env bash
set -euo pipefail
```

**Constants:**
```
PROJECT="interviewing-457222"
REGION="us-central1"
INSTANCE_NAME="spatialhub-db"
SERVICE_NAME="spatialhub-backend"
DB_NAME="spatialhub_db"
DB_USER="spatialhub"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(dirname "$SCRIPT_DIR")"
```

**Section 1: Validate prerequisites**
- Check `gcloud` is installed and authenticated: `gcloud auth print-identity-token &>/dev/null || { echo "Error: gcloud not authenticated. Run: gcloud auth login"; exit 1; }`
- Check `firebase` is installed: `command -v firebase &>/dev/null || { echo "Error: firebase CLI not found"; exit 1; }`
- Check `DB_PASS` env var is set: `[[ -n "${DB_PASS:-}" ]] || { echo "Error: DB_PASS env var required"; exit 1; }`
- Generate SECRET_KEY if not provided: `SECRET_KEY="${SECRET_KEY:-$(python3 -c "import secrets; print(secrets.token_urlsafe(50))")}"` then echo the generated key and warn user to store it.

**Section 2: Provision Cloud SQL (idempotent)**
- Guard: `if ! gcloud sql instances describe "$INSTANCE_NAME" --project="$PROJECT" &>/dev/null; then`
- Create instance: `gcloud sql instances create "$INSTANCE_NAME" --database-version=POSTGRES_15 --tier=db-f1-micro --region="$REGION" --project="$PROJECT" --authorized-networks=0.0.0.0/0 --root-password="${DB_PASS}"`
- Wait for instance to be RUNNABLE: poll `gcloud sql instances describe "$INSTANCE_NAME" --project="$PROJECT" --format='value(state)'` until it returns `RUNNABLE` (max 60 attempts, 10s sleep)
- Create database: `gcloud sql databases create "$DB_NAME" --instance="$INSTANCE_NAME" --project="$PROJECT" 2>/dev/null || echo "Database already exists"`
- Create user: `gcloud sql users create "$DB_USER" --instance="$INSTANCE_NAME" --password="${DB_PASS}" --project="$PROJECT" 2>/dev/null || echo "User already exists"`
- End guard: `else echo "Cloud SQL instance $INSTANCE_NAME already exists, skipping provisioning"; fi`

**Section 3: Get Cloud SQL public IP**
```
DB_HOST=$(gcloud sql instances describe "$INSTANCE_NAME" \
  --project="$PROJECT" --format='value(ipAddresses[0].ipAddress)')
echo "Cloud SQL IP: $DB_HOST"
```

**Section 4: Deploy Django to Cloud Run (source-based)**
```
cd "$REPO_ROOT"
gcloud run deploy "$SERVICE_NAME" \
  --source . \
  --region "$REGION" \
  --project "$PROJECT" \
  --platform managed \
  --allow-unauthenticated \
  --set-env-vars "DB_HOST=${DB_HOST},DB_NAME=${DB_NAME},DB_USER=${DB_USER},DB_PASS=${DB_PASS},SECRET_KEY=${SECRET_KEY},DEBUG=False,ALLOWED_HOSTS=*"
```

**Section 5: Get Cloud Run URL**
```
CLOUD_RUN_URL=$(gcloud run services describe "$SERVICE_NAME" \
  --region "$REGION" --project "$PROJECT" --format 'value(status.url)')
echo "Cloud Run URL: $CLOUD_RUN_URL"
```

**Section 6: Run migrations from local machine**
The Cloud SQL public IP is open (authorized-networks=0.0.0.0/0) so the deploy script can run migrate directly from the local machine against the Cloud SQL instance. This avoids Cloud Run Jobs complexity.

```
cd "$REPO_ROOT/django_backend"

# Activate local venv if it exists (for macOS dev machine)
if [[ -f venv_local/bin/activate ]]; then
  source venv_local/bin/activate
elif [[ -f venv/bin/activate ]]; then
  source venv/bin/activate
fi

echo "Running migrations against Cloud SQL..."
USE_SQLITE=0 DB_HOST="$DB_HOST" DB_NAME="$DB_NAME" DB_USER="$DB_USER" DB_PASS="$DB_PASS" \
  SECRET_KEY="$SECRET_KEY" \
  python manage.py migrate --no-input

echo "Seeding habitat zones..."
USE_SQLITE=0 DB_HOST="$DB_HOST" DB_NAME="$DB_NAME" DB_USER="$DB_USER" DB_PASS="$DB_PASS" \
  SECRET_KEY="$SECRET_KEY" \
  python manage.py seed_habitat_zones
```

**Section 7: Build and deploy frontend**
```
cd "$REPO_ROOT/spatialhub-frontend"
echo "Building frontend with VITE_API_URL=${CLOUD_RUN_URL}/api"
VITE_API_URL="${CLOUD_RUN_URL}/api" npm run build
firebase deploy --only hosting --project "$PROJECT"
```

**Section 8: Smoke checks**
```
echo ""
echo "=== Smoke Checks ==="

# Check Django API
HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${CLOUD_RUN_URL}/api/enriched/")
if [[ "$HTTP_CODE" == "200" ]]; then
  echo "PASS: Django API responds 200 at ${CLOUD_RUN_URL}/api/enriched/"
else
  echo "WARN: Django API returned $HTTP_CODE at ${CLOUD_RUN_URL}/api/enriched/"
fi

# Check habitat zones seeded
ZONES=$(curl -s "${CLOUD_RUN_URL}/api/habitat/zones/" | python3 -c "import sys,json; print(len(json.load(sys.stdin)))" 2>/dev/null || echo "0")
if [[ "$ZONES" == "4" ]]; then
  echo "PASS: habitat_zones has 4 seeded zones"
else
  echo "WARN: habitat_zones returned $ZONES zones (expected 4)"
fi

# Check Firebase Hosting
FB_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://interviewing-457222.web.app")
if [[ "$FB_CODE" == "200" ]]; then
  echo "PASS: Firebase Hosting responds 200"
else
  echo "WARN: Firebase Hosting returned $FB_CODE"
fi

# Check sensor ingest endpoint
INGEST_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST "${CLOUD_RUN_URL}/api/sensor-ingest/" \
  -H "Content-Type: application/json" \
  -d '{"hub_id":"smoke-test","sensor_name":"ph","sensor_val":7.0,"device_addr":"99","datetime":"2026-01-01T00:00:00Z","sensor_id":"smoke-99","collection_type":"sensor_data"}')
if [[ "$INGEST_CODE" == "201" ]]; then
  echo "PASS: POST /api/sensor-ingest/ returns 201"
else
  echo "WARN: POST /api/sensor-ingest/ returned $INGEST_CODE"
fi

echo ""
echo "=== Deployment Complete ==="
echo "Cloud Run:  ${CLOUD_RUN_URL}"
echo "Frontend:   https://interviewing-457222.web.app"
echo "Cloud SQL:  ${DB_HOST}"
echo ""
echo "IMPORTANT: Store these values for future redeploys:"
echo "  SECRET_KEY=${SECRET_KEY}"
echo "  DB_PASS=${DB_PASS}"
```

Make the script executable: `chmod +x deploy/deploy.sh`

Then **execute the deploy script**:
```bash
cd /Users/nicolasdemari/dev/SpatialHub-JobDemoClone
DB_PASS="<generate a strong password>" ./deploy/deploy.sh
```

Wait for the full deployment to complete. The script will output smoke check results. If any smoke check fails, diagnose and fix before proceeding.

**Troubleshooting guide if issues arise:**
- 403 on POST: Check CSRF_TRUSTED_ORIGINS has `https://*.run.app`
- 400 Bad Request: Check ALLOWED_HOSTS -- should be `*` via env var
- Connection refused on migrate: Cloud SQL instance not yet RUNNABLE -- wait and retry
- Module not found (psycopg2): Check requirements.txt has psycopg2-binary
- Frontend shows localhost errors: Verify VITE_API_URL was exported before npm run build
  </action>
  <verify>
    <automated>curl -sf "$(gcloud run services describe spatialhub-backend --region us-central1 --project interviewing-457222 --format 'value(status.url)')/api/habitat/zones/" | python3 -c "import sys,json; data=json.load(sys.stdin); assert len(data)==4, f'Expected 4 zones, got {len(data)}'; print('PASS: 4 zones seeded')"</automated>
  </verify>
  <done>
- deploy/deploy.sh exists, is executable, and ran successfully
- Cloud SQL instance "spatialhub-db" exists in us-central1 with db-f1-micro tier
- All Django tables migrated on Cloud SQL
- habitat_zones table has 4 seeded zones (GET /api/habitat/zones/ returns 4 items)
- Django API accessible at Cloud Run URL (GET /api/enriched/ returns 200)
- POST /api/sensor-ingest/ returns 201 and stores row in Cloud SQL
- Frontend accessible at https://interviewing-457222.web.app
- All smoke checks in deploy script passed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cloud deployment in browser</name>
  <action>
Human verifies the full cloud deployment works correctly in a browser. All automated deployment and smoke checks have already been completed by Task 1.

What was built: Full cloud deployment -- Django API on Cloud Run, Cloud SQL database with migrations and seed data, frontend on Firebase Hosting. All pages should load and display data correctly from the cloud database.

How to verify:
1. Open https://interviewing-457222.web.app in your browser
2. Verify the main navigation loads (should see links for Raw, Enriched, Trends, Simulate, Mars Habitat)
3. Click "Mars Habitat" -- the 3D habitat should load with BioSim fallback mode (amber badge) since no BioSim VM exists yet
4. Click "Enriched" -- should load data from Cloud SQL (may be empty if no prior data, or show the smoke-test row)
5. Click "Trends" -- should load without URL errors (may show no data yet)
6. Click "Raw" -- should load without URL errors
7. Open the Cloud Run URL directly in browser: {CLOUD_RUN_URL}/api/habitat/zones/ -- should return JSON with 4 habitat zones
8. Verify the deploy script output showed all smoke checks as PASS
  </action>
  <verify>
    <automated>curl -sf "https://interviewing-457222.web.app" -o /dev/null && echo "PASS: Firebase responds" || echo "FAIL: Firebase not responding"</automated>
  </verify>
  <done>Human confirmed all pages load correctly and data displays from Cloud SQL</done>
</task>

</tasks>

<verification>
1. Cloud SQL instance exists: `gcloud sql instances describe spatialhub-db --project=interviewing-457222 --format='value(state)'` returns RUNNABLE
2. Cloud Run service responds: `curl -f "${CLOUD_RUN_URL}/api/enriched/"` returns 200
3. Habitat zones seeded: `curl -s "${CLOUD_RUN_URL}/api/habitat/zones/"` returns JSON array with 4 items
4. Sensor ingest works: `curl -X POST "${CLOUD_RUN_URL}/api/sensor-ingest/" -H "Content-Type: application/json" -d '{"hub_id":"test","sensor_name":"ph","sensor_val":7.0,"device_addr":"99","datetime":"2026-01-01T00:00:00Z","sensor_id":"t-99","collection_type":"sensor_data"}'` returns 201
5. Firebase Hosting live: `curl -f "https://interviewing-457222.web.app"` returns 200
6. Human verified: All pages load correctly in browser
</verification>

<success_criteria>
- deploy/deploy.sh is idempotent and executable
- Django API live at Cloud Run URL, serving data from Cloud SQL
- Frontend live at Firebase Hosting URL, connecting to Cloud Run API
- Cloud SQL has all tables migrated and 4 habitat zones seeded
- POST /api/sensor-ingest/ works on Cloud Run
- Human verified all pages load correctly in browser
</success_criteria>

<output>
After completion, create `.planning/phases/11-cloud-services-deployment/11-02-SUMMARY.md`
</output>
