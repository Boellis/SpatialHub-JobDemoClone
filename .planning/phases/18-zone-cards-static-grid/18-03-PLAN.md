---
phase: 18-zone-cards-static-grid
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified: []
autonomous: false
requirements: [LAYOUT-02, DATA-01, DATA-02, DATA-03, DATA-04, STAT-01, STAT-02]

must_haves:
  truths:
    - "Hero card spans 2 columns at 55% viewport height with 48px bold sensor values"
    - "3 secondary cards in bottom row with 28px bold sensor values"
    - "Each sensor row renders a Sparkline SVG that updates with live history data"
    - "Status bar shows 'SOL NNN' center and connection badge right with colored dot"
    - "Red zone cards animate with 2s zoneCriticalPulse border glow cycle"
    - "Yellow zone cards have static rgba(255,170,0,0.4) amber border, no animation"
---

<objective>
Visual verification checkpoint: confirm the TV dashboard renders correctly in a browser with live telemetry data.

Purpose: This is a human-verify checkpoint -- automated tests confirm structural correctness, but only a human can verify the visual result looks right on screen (typography readability, grid proportions, sparkline rendering, pulse animation visibility).

Output: Human approval or issue list for gap closure
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-zone-cards-static-grid/18-CONTEXT.md
@.planning/phases/18-zone-cards-static-grid/18-UI-SPEC.md
@.planning/phases/18-zone-cards-static-grid/18-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of TV dashboard</name>
  <files></files>
  <action>
Start dev server with `cd spatialhub-frontend && npm run dev`, then present the verification checklist to the user. The user opens http://localhost:5173/habitat and visually inspects the dashboard against the Phase 18 requirements.
  </action>
  <verify>User confirms all visual checks pass by typing "approved"</verify>
  <done>Human has visually verified the TV dashboard meets all Phase 18 requirements at TV viewing distance.</done>
  <what-built>
Complete TV dashboard with priority-ordered zone cards, status bar, sparkline charts, and critical zone pulse animation. Plans 01 and 02 implemented all components: StatusBar (sol counter, connection badge, alert messaging), ZoneCard (sensor rows, sparklines, status-reactive borders, critical pulse), PriorityGrid (hero + 3 secondary CSS Grid layout), and TvDashboardView rewire (debug overlay removed).
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd spatialhub-frontend && npm run dev`
2. Open `http://localhost:5173/habitat` in a browser
3. Wait 4-6 seconds for telemetry to start flowing

**Check LAYOUT-02 (priority grid):**
- [ ] Hero card spans full width on top row (visually larger)
- [ ] 3 secondary cards in bottom row, evenly spaced
- [ ] 24px gap between cards, 32px padding from viewport edges

**Check DATA-01 (TV-safe typography):**
- [ ] Hero card sensor values are large (48px), readable from 3+ meters
- [ ] Secondary card sensor values are smaller (28px) but still readable
- [ ] All text is Space Mono monospace font

**Check DATA-02 (sparkline charts):**
- [ ] Each sensor row has a sparkline on the right side
- [ ] Hero sparklines are wider than secondary sparklines
- [ ] Sparklines update as new data arrives (watch for 10+ seconds)

**Check DATA-03 (sol counter):**
- [ ] Status bar at top shows "SOL 000" (or incrementing number)
- [ ] Format is "SOL" followed by 3-digit zero-padded number

**Check DATA-04 (connection badge):**
- [ ] Right side of status bar shows data source (e.g., "Fallback Mode" or "BioSim Live")
- [ ] Colored dot precedes the label text

**Check STAT-01 (status summary bar):**
- [ ] When all zones are green: "ALL SYSTEMS NOMINAL" with green dot
- [ ] If any zone goes yellow/red: zone name + status shown (e.g., "WATER RECYCLING: CAUTION")

**Check STAT-02 (critical zone pulse):**
- [ ] If a zone is red, its card border pulses with a red glow (2s cycle)
- [ ] Green zone cards have nearly invisible borders
- [ ] Yellow zone cards have static amber borders (no animation)
- [ ] To force a red zone for testing: open browser DevTools console and run `window.__HABITAT_STORE?.setState(s => ({ zones: { ...s.zones, 'water-recycling': { ...s.zones['water-recycling'], status: 'red' } } }))` — or alternatively, edit `src/simulation/constants.ts` temporarily to set a sensor's `nominalValue` far outside its thresholds, then restart dev server

**Overall glance test:**
- [ ] Dashboard fills the full viewport (no scrollbars)
- [ ] Dark background (#06070b), no white flash on load
- [ ] No interactive cursors or hover states visible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all Phase 18 requirements at TV viewing distance.
</verification>

<success_criteria>
All visual checks pass. Dashboard is glanceable from across the room. No layout issues, typography problems, or missing functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/18-zone-cards-static-grid/18-03-SUMMARY.md`
</output>