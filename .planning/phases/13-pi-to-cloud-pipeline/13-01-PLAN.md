---
phase: 13-pi-to-cloud-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - spatialhub-frontend/src/hooks/useLiveSensors.ts
  - spatialhub-frontend/src/__tests__/useLiveSensors.test.ts
  - hubcode/.env.example
autonomous: false
requirements: [DEPLOY-05]

must_haves:
  truths:
    - "PI_HUB_ID constant in useLiveSensors.ts is 'pi-habitat-01' — matching what the Pi client actually sends"
    - "SENSOR_MAP key 'ph' matches the SENSOR_NAME value documented in .env.example — no case mismatch silently dropping readings"
    - ".env.example documents Cloud Run URL as the production DJANGO_URL — a judge can copy the template and point their Pi at the cloud"
    - "Frontend is rebuilt and redeployed to Firebase with the PI_HUB_ID fix — the live site at nasa-comp-demo.web.app uses the corrected constant"
    - "Pi posts real pH readings to Cloud Run endpoint and rows appear in Cloud SQL with hub_id='pi-habitat-01'"
    - "Offline SQLite buffer retains readings when Cloud Run is unreachable and syncs when connectivity restores"
  artifacts:
    - path: "spatialhub-frontend/src/hooks/useLiveSensors.ts"
      provides: "Corrected PI_HUB_ID constant"
      contains: "pi-habitat-01"
    - path: "spatialhub-frontend/src/__tests__/useLiveSensors.test.ts"
      provides: "Unit test asserting PI_HUB_ID value and SENSOR_MAP keys"
    - path: "hubcode/.env.example"
      provides: "Production config template with Cloud Run URL and correct SENSOR_NAME=ph"
      contains: "spatialhub-backend"
  key_links:
    - from: "hubcode/hub_client.py"
      to: "Cloud Run /api/sensor-ingest/"
      via: "DJANGO_URL from .env"
      pattern: "requests\\.post.*sensor-ingest"
    - from: "spatialhub-frontend/src/hooks/useLiveSensors.ts"
      to: "Cloud Run /api/enriched/?hub_id=pi-habitat-01"
      via: "PI_HUB_ID constant in fetch URL"
      pattern: "PI_HUB_ID.*pi-habitat-01"
    - from: "hubcode/.env SENSOR_NAME"
      to: "useLiveSensors.ts SENSOR_MAP"
      via: "exact string match on 'ph'"
      pattern: "ph.*water-recycling"
---

<objective>
Fix the hub_id mismatch in the frontend, align the Pi config template for cloud deployment, rebuild/redeploy the frontend, and verify the full Pi-to-Cloud pipeline end-to-end on real hardware.

Purpose: This is the bridge between "Pi code works locally" (Phase 10) and "Pi data flows through the cloud" (Phase 13 success criteria). The hub_client.py code is already correct -- the work is one constant fix, one config template update, a Firebase redeploy, and a physical hardware verification.

Output: Corrected useLiveSensors.ts, updated .env.example, redeployed Firebase frontend, verified Pi-to-Cloud SQL data flow.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pi-to-cloud-pipeline/13-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From spatialhub-frontend/src/hooks/useLiveSensors.ts:
```typescript
const PI_HUB_ID = '9c9Kfeo4SK7BW4hw8dvQ';  // BUG: must be 'pi-habitat-01'

const SENSOR_MAP: Record<string, { zoneId: string; sensorId: string }> = {
  ph: { zoneId: 'water-recycling', sensorId: 'wr-ph' },  // key must match Pi SENSOR_NAME
};

export function useLiveSensors(): void;
```

From spatialhub-frontend/src/api/api.ts:
```typescript
export const BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api';
```

From hubcode/hub_client.py:
```python
SENSOR_NAME = os.environ.get("SENSOR_NAME", "")  # loaded from .env
DJANGO_URL = os.environ.get("DJANGO_URL", "")     # loaded from .env
# sync_readings() posts to: f"{DJANGO_URL}/api/sensor-ingest/"
```

Cloud Run URL (from Phase 11): https://spatialhub-backend-4vovlomqfa-uc.a.run.app
Firebase URL (from Phase 11): https://nasa-comp-demo.web.app
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PI_HUB_ID, align SENSOR_NAME, update .env.example, add test</name>
  <files>
    spatialhub-frontend/src/hooks/useLiveSensors.ts,
    spatialhub-frontend/src/__tests__/useLiveSensors.test.ts,
    hubcode/.env.example
  </files>
  <action>
    **1. Fix PI_HUB_ID in useLiveSensors.ts (line 14):**
    Change `const PI_HUB_ID = '9c9Kfeo4SK7BW4hw8dvQ';` to `const PI_HUB_ID = 'pi-habitat-01';`
    This is the critical fix -- without it, the frontend polls for a hub_id that the Pi never sends, so the LIVE badge never appears and Pi data never surfaces in the habitat view.

    Export PI_HUB_ID so the test can import it: change from `const PI_HUB_ID` to `export const PI_HUB_ID`.

    **2. Create useLiveSensors.test.ts:**
    Create `spatialhub-frontend/src/__tests__/useLiveSensors.test.ts` with Vitest. Add `/** @vitest-environment jsdom */` directive at top (same pattern as useSimSource.test.ts).

    Test cases:
    - Assert `PI_HUB_ID === 'pi-habitat-01'` (regression guard against the old provisioned ID)
    - Assert `SENSOR_MAP` has key `'ph'` mapping to `{ zoneId: 'water-recycling', sensorId: 'wr-ph' }` (regression guard against case mismatch)

    Import `PI_HUB_ID` and `SENSOR_MAP` from `'../hooks/useLiveSensors'`. Note: SENSOR_MAP will also need to be exported from useLiveSensors.ts for the test to import it.

    **3. Update hubcode/.env.example:**
    Update the DJANGO_URL line to show both local and cloud options:
    ```
    # Django API URL (local development)
    # DJANGO_URL=http://localhost:8000
    # Django API URL (Cloud Run production -- use this for NASA competition)
    DJANGO_URL=https://spatialhub-backend-4vovlomqfa-uc.a.run.app
    ```

    Ensure SENSOR_NAME is set to `ph` (lowercase, no spaces) to match the SENSOR_MAP key in useLiveSensors.ts:
    ```
    SENSOR_NAME=ph
    ```
    If the current .env.example has a different value (e.g., "pH Sensor"), change it to `ph`. Add a comment explaining why: `# Must match frontend SENSOR_MAP key -- use lowercase 'ph', not 'pH Sensor'`

    Keep all other fields as they are (HUB_ID=pi-habitat-01, SENSOR_ID=wr-ph-real, DEVICE_ADDR=99, etc.).

    **Do NOT:**
    - Modify hub_client.py itself -- it reads from .env and needs no code changes
    - Modify atlas_i2c.py -- no changes needed
    - Change the POLL_INTERVAL_MS or any other constant in useLiveSensors.ts
    - Disable SSL verification anywhere
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/SpatialHub-JobDemoClone/spatialhub-frontend && npx vitest run src/__tests__/useLiveSensors.test.ts</automated>
  </verify>
  <done>
    - PI_HUB_ID is 'pi-habitat-01' in useLiveSensors.ts
    - SENSOR_MAP exported and tested with key 'ph' -> water-recycling/wr-ph
    - .env.example shows Cloud Run URL as default DJANGO_URL and SENSOR_NAME=ph
    - useLiveSensors.test.ts passes with both assertions green
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild frontend and redeploy to Firebase</name>
  <files>spatialhub-frontend/</files>
  <action>
    The PI_HUB_ID fix from Task 1 is a compile-time constant baked into the Vite bundle. The live Firebase site will keep using the OLD value until we rebuild and redeploy.

    **1. Build the frontend:**
    ```bash
    cd spatialhub-frontend
    VITE_API_URL=https://spatialhub-backend-4vovlomqfa-uc.a.run.app/api \
    VITE_BIOSIM_URL=wss://34-68-135-16.sslip.io \
    VITE_OPENMCT_URL=http://34.68.135.16:9091 \
    npm run build
    ```
    These env vars match what was used in Phase 12 deployment. The VITE_API_URL ensures BASE_URL in api.ts points to Cloud Run. The VITE_BIOSIM_URL ensures WebSocket connects to the GCE VM via Caddy HTTPS proxy.

    **2. Deploy to Firebase:**
    ```bash
    npx firebase deploy --only hosting --project nasa-comp-demo
    ```

    **3. Verify the deployment:**
    ```bash
    curl -s https://nasa-comp-demo.web.app | head -5
    ```
    Should return HTML (not a 404 or error).

    **Do NOT:**
    - Change any other frontend code beyond what Task 1 modified
    - Modify firebase.json or .firebaserc
    - Use different env var values than listed above (they must match Phase 12)
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://nasa-comp-demo.web.app</automated>
  </verify>
  <done>
    - Firebase hosting at nasa-comp-demo.web.app serves the rebuilt frontend with PI_HUB_ID='pi-habitat-01' baked in
    - curl returns 200 for the Firebase URL
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Pi-to-Cloud pipeline on real hardware</name>
  <files>hubcode/.env (on Pi filesystem at 10.0.0.161)</files>
  <action>
    Human verifies the end-to-end pipeline on physical Pi hardware. Claude cannot do this -- it requires SSH to a Pi on the local network, physical Atlas Scientific sensor readings, and WiFi toggle testing.

    **What was built:** Fixed PI_HUB_ID constant, aligned SENSOR_NAME in .env.example, rebuilt and redeployed frontend to Firebase. The full Pi-to-Cloud pipeline should now work end-to-end.

    **Pi Configuration (on the Pi at 10.0.0.161):**
    1. SSH into the Pi: `ssh pi@10.0.0.161`
    2. Check the hub_client .env: `cat ~/hubcode/.env` (or wherever hub_client.py lives)
    3. Confirm DJANGO_URL is `https://spatialhub-backend-4vovlomqfa-uc.a.run.app`
    4. Confirm SENSOR_NAME is `ph` (lowercase, no spaces)
    5. Confirm HUB_ID is `pi-habitat-01`
    6. If any value is wrong, update the .env on the Pi

    **Live Data Flow:**
    7. Start hub_client on Pi: `cd ~/hubcode && python hub_client.py -v`
    8. Watch for "synced" log lines (first sync may take 2-3s due to Cloud Run cold start)
    9. Verify data arrived in Cloud SQL:
       `curl "https://spatialhub-backend-4vovlomqfa-uc.a.run.app/api/enriched/?hub_id=pi-habitat-01" | python3 -m json.tool | head -40`
       Expected: JSON with hub_id='pi-habitat-01' and sensor_name='ph'

    **Frontend LIVE Badge:**
    10. Visit https://nasa-comp-demo.web.app/habitat
    11. Navigate to the Water Recycling zone
    12. Confirm the LIVE badge appears on the pH sensor (indicates real Pi data is flowing)

    **Offline Buffer Test:**
    13. On the Pi, kill WiFi: `sudo ifconfig wlan0 down`
    14. Watch hub_client logs -- should show "buffered (offline)"
    15. Restore WiFi: `sudo ifconfig wlan0 up`
    16. Watch for "synced (+ N buffered)" confirming backlog flush
  </action>
  <verify>
    <automated>curl -s "https://spatialhub-backend-4vovlomqfa-uc.a.run.app/api/enriched/?hub_id=pi-habitat-01" | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS' if isinstance(d,list) and len(d)>0 else 'FAIL: no Pi rows')"</automated>
  </verify>
  <done>
    - Pi .env on hardware points to Cloud Run URL with SENSOR_NAME=ph and HUB_ID=pi-habitat-01
    - hub_client.py posts real pH to Cloud Run and rows appear in Cloud SQL
    - LIVE badge visible on /habitat Water Recycling zone
    - Offline buffer retains readings during WiFi loss and syncs on restore
  </done>
</task>

</tasks>

<verification>
Phase 13 is verified when:
1. `useLiveSensors.test.ts` passes (PI_HUB_ID and SENSOR_MAP assertions green)
2. Existing hubcode unit tests still pass: `cd hubcode && python -m unittest discover tests/`
3. Firebase site is live with the fixed PI_HUB_ID constant
4. Pi data rows visible in Cloud SQL via `GET /api/enriched/?hub_id=pi-habitat-01`
5. LIVE badge appears on /habitat Water Recycling zone when Pi is posting
6. Offline buffer tested and confirmed working
</verification>

<success_criteria>
- PI_HUB_ID is 'pi-habitat-01' in source and deployed Firebase bundle
- Pi `.env` on SD card points to Cloud Run URL
- Real pH readings appear in Cloud SQL `enriched_sensor_data` with hub_id='pi-habitat-01'
- `GET {cloud-run-url}/api/enriched/?hub_id=pi-habitat-01` returns Pi data
- Offline SQLite buffer retains readings during connectivity loss and syncs on restore
- LIVE badge visible in habitat view when Pi data is flowing
</success_criteria>

<output>
After completion, create `.planning/phases/13-pi-to-cloud-pipeline/13-01-SUMMARY.md`
</output>
